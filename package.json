{
  "name": "stringer-news-portal",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "collect": "node scripts/collect.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check app.js && node --check news-model.js && node --check scripts/collect.mjs",
    "test:browser": "node --test tests/browser.spec.mjs"
  },
  "dependencies": {
    "cheerio": "1.2.0",
    "fast-xml-parser": "5.11.1"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}
